#include "pxt.h"
#include "plarail.h"

namespace plarail {
    void sendIR(int id, int dir, int speed) {
        // 実装例：IRフォーマット生成＆IR-LED送信（P1接続）
        // 送信タイミングやPWM出力はここに記述
        // ★この関数はMakeCodeから呼び出されます
    }
}
